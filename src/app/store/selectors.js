// Selectors
import { selectors as configs } from './configs';
import { selectors as constituencies } from './constituencies';
import { selectors as deputies } from './deputies';

export default {
  configs,
  constituencies,
  deputies,
};
